---
title: "randomInt"
description: "Generates a random integer within a specified range."
---

import { Badge, Field } from '@ciderpress/ui/theme'

# randomInt <Badge color="#c85a3e">es-toolkit</Badge>

Generates a random integer within a specified range.

```typescript
const randomInteger = randomInt(min, max);
```

## Usage

### `randomInt(maximum)` / `randomInt(minimum, maximum)`

Use `randomInt` when you need a random integer. It returns only integers without decimal points.

```typescript
import { randomInt } from 'es-toolkit/math';

// Generate a random integer between 0 and 5 (exclusive).
const num1 = randomInt(5);
console.log(num1); // e.g., 3

// Generate a random integer between 2 and 10 (exclusive).
const num2 = randomInt(2, 10);
console.log(num2); // e.g., 7

// Can be used with negative ranges.
const num3 = randomInt(-5, -1);
console.log(num3); // e.g., -3

// Simulate a dice roll (1-6)
const diceRoll = randomInt(1, 7);
console.log(diceRoll); // e.g., 4

// Select a random index from an array
const items = ['apple', 'banana', 'cherry', 'date'];
const randomIndex = randomInt(items.length);
console.log(items[randomIndex]); // e.g., 'banana'
```

#### Parameters

<Field name="maximum" type="number" required>
The maximum value (exclusive) when using a single parameter. Must be greater than 0.
</Field>

<Field name="minimum" type="number" required>
The minimum value (inclusive).
</Field>

<Field name="maximum" type="number" required>
The maximum value (exclusive). Must be greater than the minimum value.
</Field>

#### Returns

Returns a random integer within the specified range.

```typescript
number
```

#### Throws

Throws an error if the maximum value is less than or equal to the minimum value.

<details>
<summary>Source: es-toolkit</summary>

Re-exported verbatim from [`es-toolkit`](https://es-toolkit.dev/reference/math/randomInt.html). Implementation, edge cases, and performance behavior are owned upstream. This page mirrors the documentation at the pinned version; the linked source is authoritative.

</details>
