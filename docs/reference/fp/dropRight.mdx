---
title: "dropRight"
description: "Creates a function that drops values from the end of an array."
---

import { Badge, Field } from '@ciderpress/ui/theme'

# dropRight (Functional Programming) <Badge color="#c85a3e">es-toolkit</Badge>

Creates a function that drops values from the end of an array. Use it with [`pipe`](./pipe.md).

```typescript
const result = pipe(array, dropRight(count));
```

::: info

Prefer the original es-toolkit [`dropRight`](../../reference/array/dropRight.md) in ordinary code. Use this `fp` variant when composing transformations with [`pipe`](./pipe.md).

:::

## Usage

`dropRight` removes the last `count` values from the piped array.

```typescript
import { dropRight, pipe } from 'es-toolkit/fp';

pipe([1, 2, 3, 4], dropRight(2)); // => [1, 2]
```

#### Parameters

<Field name="count" type="number" required>
The number of values to drop from the end.
</Field>

#### Returns

A function that maps a `readonly T[]` to the remaining values.

```typescript
(array: readonly T[]) => T[]
```

<details>
<summary>Source: es-toolkit</summary>

Re-exported verbatim from [`es-toolkit`](https://es-toolkit.dev/fp/reference/dropRight.html). Implementation, edge cases, and performance behavior are owned upstream. This page mirrors the documentation at the pinned version; the linked source is authoritative.

</details>
