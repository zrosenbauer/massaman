---
title: "intersection"
description: "Creates a function that keeps values found in another array."
---

import { Badge, Field } from '@ciderpress/ui/theme'

# intersection (Functional Programming) <Badge color="#c85a3e">es-toolkit</Badge>

Creates a function that keeps values found in another array. Use it with [`pipe`](./pipe.md).

```typescript
const result = pipe(array, intersection(secondArray));
```

::: info

Prefer the original es-toolkit [`intersection`](../../reference/array/intersection.md) in ordinary code. Use this `fp` variant when composing transformations with [`pipe`](./pipe.md).

:::

## Usage

`intersection` keeps values from the piped array that are also present in `secondArray`. It preserves the order of the piped array.

```typescript
import { intersection, pipe } from 'es-toolkit/fp';

pipe([1, 2, 3], intersection([2, 3, 4])); // => [2, 3]
```

#### Parameters

<Field name="secondArray" type="readonly T[]" required>
The array containing values to keep.
</Field>

#### Returns

A function that maps a `readonly T[]` to values also found in `secondArray`.

```typescript
(array: readonly T[]) => T[]
```

<details>
<summary>Source: es-toolkit</summary>

Re-exported verbatim from [`es-toolkit`](https://es-toolkit.dev/fp/reference/intersection.html). Implementation, edge cases, and performance behavior are owned upstream. This page mirrors the documentation at the pinned version; the linked source is authoritative.

</details>
