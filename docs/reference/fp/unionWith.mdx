---
title: "unionWith"
description: "Creates a function that combines arrays using a custom equality function."
---

import { Badge, Field } from '@ciderpress/ui/theme'

# unionWith (Functional Programming) <Badge color="#c85a3e">es-toolkit</Badge>

Creates a function that combines arrays using a custom equality function. Use it with [`pipe`](./pipe.md).

```typescript
const result = pipe(array, unionWith(secondArray, areItemsEqual));
```

::: info

Prefer the original es-toolkit [`unionWith`](../../reference/array/unionWith.md) in ordinary code. Use this `fp` variant when composing transformations with [`pipe`](./pipe.md).

:::

## Usage

`unionWith` keeps the first value from the combined arrays for which `areItemsEqual` does not match an already kept value.

```typescript
import { pipe, unionWith } from 'es-toolkit/fp';

pipe(
  [{ id: 1 }, { id: 2 }],
  unionWith([{ id: 2 }, { id: 3 }], (a, b) => a.id === b.id)
); // => [{ id: 1 }, { id: 2 }, { id: 3 }]
```

#### Parameters

<Field name="secondArray" type="readonly T[]" required>
The array to combine after the piped array.
</Field>

<Field name="areItemsEqual" type="(item: T, other: T) =&gt; boolean" required>
The function that decides whether two values are equal.
</Field>

#### Returns

A function that maps a `readonly T[]` to a union by custom equality.

```typescript
(array: readonly T[]) => T[]
```

<details>
<summary>Source: es-toolkit</summary>

Re-exported verbatim from [`es-toolkit`](https://es-toolkit.dev/fp/reference/unionWith.html). Implementation, edge cases, and performance behavior are owned upstream. This page mirrors the documentation at the pinned version; the linked source is authoritative.

</details>
